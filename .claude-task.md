# Task: Fix TypeScript errors and polish React migration

The project was migrated from single-file HTML to React + TypeScript + Vite + Tailwind.
Run `npx tsc -b` to see all current TypeScript errors. Fix every single one.

## TypeScript Fixes Required:

1. Content.tsx line 2: wrong import path `../../utils/types` — should be `../utils/types`
2. Content.tsx line 72: `EggView` missing — check if it's imported
3. CopilotPanel.tsx: add proper TypeScript props interface for `isOpen`, `onClose`, `molecules`
4. Header.tsx: remove unused `useState` import
5. Sidebar.tsx line 123: fix `setCompareIndices` call — it's passing a function where array is expected. Fix the type or the call.
6. CompareView.tsx: remove unused imports
7. ParallelView.tsx line 188: prefix unused `e` with underscore
8. ParetoView.tsx: remove unused `activeFilters` and `i` variables
9. RadarView.tsx: remove unused `useRef` import
10. TableView.tsx: remove unused `colMol` variable

After fixing ALL TypeScript errors, verify the build passes: `npx tsc -b && npx vite build`

## Then update CLAUDE.md to reflect new React architecture:

Update the CLAUDE.md file to describe the new project structure:
- React 19 + TypeScript + Vite 8 + Tailwind CSS 3
- Component structure: App.tsx → Header, Sidebar, Content, CopilotPanel
- Views: ParetoView, ScoringView, ParallelView, EggView, RadarView, TableView, CompareView
- Utils: chem.ts (RDKit WASM integration, PubChem lookup), types.ts (interfaces)
- Design system: dark theme, Geist font, warm zinc palette (#09090b bg, #1A1918 surfaces, #798F81 accents)

Then: git add -A && git commit -m "fix: resolve all TypeScript errors, update CLAUDE.md for React architecture" && git push origin main
