# v15 Task: Refine MolParetoLab

Read CLAUDE.md for project context. Then make ALL of the following changes to index.html:

## 1. PubChem Lookup UX Enhancement
- In the analyze() function, after PubChem resolves names, the textarea should update BEFORE analysis starts so the user sees the resolved SMILES
- Add a small "Resolving names via PubChem..." animated text near the status area during lookup
- If ALL names fail to resolve, show a helpful error: "Could not find these molecules on PubChem. Try pasting SMILES directly."

## 2. Placeholder Text Update
- Change the textarea placeholder to show that names work too:
```
paste SMILES or drug names (one per line)

aspirin
ibuprofen
CC(=O)Oc1ccccc1C(=O)O acetylsalicylic acid
```

## 3. Add "Drug Names" Quick Load Example
- Add a 5th example button after "Kinase inhibitors" labeled "Type drug names — no SMILES needed"
- Its EXAMPLES entry should be just plain drug names: "aspirin\nibuprofen\nacetaminophen\ncaffeine\nmetformin\natorvastatin\nomeprazole\nloratadine"
- This demonstrates the PubChem lookup feature

## 4. Copilot: Auto-open on First Analysis
- After the first successful analysis (molecules.length > 0 for the first time), if the copilot has never been opened, show a subtle pulse animation on the FAB for 3 seconds to draw attention
- Add CSS: .copilot-fab.pulse { animation: fabPulse 0.6s ease-in-out 5; }
- @keyframes fabPulse { 0%,100% { transform: scale(1); } 50% { transform: scale(1.15); } }

## 5. Consistent Button Styling in Header
- The Export CSV button should match the other btn-share buttons (check it currently uses btn-share from v14 -- verify and fix if needed)

## 6. Footer / Attribution
- Add a subtle footer at the very bottom of the page (below everything, inside the body but after the main div):
```html
<div style="text-align:center;padding:12px;font-size:11px;color:var(--text2);opacity:0.5">
  Built by <a href="https://ilkham.com" target="_blank" style="color:var(--text2)">Ilkham Yabbarov</a> · 
  Client-side only · <a href="https://github.com/IlkhamFY/molparetolab" target="_blank" style="color:var(--text2)">Open Source</a>
</div>
```

After ALL changes: git add -A && git commit -m "v15: PubChem UX polish, drug names example, copilot pulse, footer" && git push origin main
